`docs/TASKS.md` を読み、指定されたタスク(引数がなければ「進行中」の先頭タスク)について、着手前に以下を行う:

1. タスクの完了条件が明記されているか確認する。曖昧な場合はユーザーに確認するか `[要確認: 内容]` として明示する
2. 変更が必要になりそうなファイルを洗い出す
3. 作業手順を箇条書きで提示する(実装はまだしない)
4. `docs/ARCHITECTURE.md` に反する設計変更が必要な場合は、その旨を明示しユーザーの判断を仰ぐ

引数: $ARGUMENTS (対象タスクの説明。省略時は`docs/TASKS.md`の「進行中」先頭タスク)
