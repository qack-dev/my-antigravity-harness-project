`AGENTS.md` の「開発コマンド(検証ループ)」に記載されたテスト・リント・ビルドコマンドを順に実行し、結果を報告する。

1. `AGENTS.md` からテスト/リント/ビルドコマンドを読み取る(`[要確認]`のまま残っていたら、先にユーザーへ確認する)
2. 各コマンドを実行する
3. 失敗があれば、原因と該当ファイルを特定し、修正方針を提示する(ユーザーの指示がない限り、無断で仕様を変更しない)
4. すべて通過したら、実行したコマンドと結果を簡潔に一覧で報告する
