現在のステージ済み/未ステージの変更内容を要約し、コミットメッセージ案を作成する。

1. `git status` と `git diff` で変更内容を確認する
2. 変更の性質(機能追加/修正/リファクタリング/ドキュメント等)を判定する
3. 「何を」ではなく「なぜ」を中心に、1〜2文の簡潔なコミットメッセージ案を提示する
4. ユーザーの承認を得てからコミットする(無断でコミット・pushしない)
